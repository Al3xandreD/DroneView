from pathlib import Path

ROOT        = Path(__file__).resolve().parents[2]
DATA        = ROOT / "data.nosync"
MODELS      = ROOT / "models.nosync"
OUTPUTS     = ROOT / "outputs"
CONFIGS     = ROOT / "configs"